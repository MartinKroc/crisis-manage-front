import { ShowVerificationPipe } from './show-verification.pipe';

describe('ShowVerificationPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowVerificationPipe();
    expect(pipe).toBeTruthy();
  });
});
