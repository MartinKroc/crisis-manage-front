import { AlertTypesPipe } from './alert-types.pipe';

describe('AlertTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new AlertTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
